import { AdditionPipePipe } from './addition-pipe.pipe';

describe('AdditionPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AdditionPipePipe();
    expect(pipe).toBeTruthy();
  });
});
