import { ReverseCustomPipePipe } from './reverse-custom-pipe.pipe';

describe('ReverseCustomPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ReverseCustomPipePipe();
    expect(pipe).toBeTruthy();
  });
});
